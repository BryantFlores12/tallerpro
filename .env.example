PORT=4000
NODE_ENV=development

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=tallerpro
DB_PASSWORD=replace-with-a-strong-database-password
DB_NAME=tallerpro

JWT_SECRET=replace-with-at-least-32-random-characters
JWT_EXPIRES=8h
BCRYPT_ROUNDS=12

FRONTEND_URL=http://localhost:4000

# Required only when running npm run db:reset:seed
SEED_PASSWORD=replace-with-a-unique-demo-password
